name: "verify: backend"

on:
  pull_request:
    branches:
      - 'main'
    paths:
      - 'apps/backend/**'
      - '.github/workflows/verify-backend.yaml'
      - 'apps/backend/pom.xml'
  workflow_dispatch:

jobs:
  verify:
    name: Verify backend compiles and run tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # ratchet:actions/checkout@v6
      - name: Cache Dependencies
        uses: actions/cache@2c8a9bd7457de244a408f35966fab2fb45fda9c8 # ratchet:actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - name: JDK
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # ratchet:actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: 25
      - name: Run tests
        env:
          GITHUB_USERNAME: x-access-token
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mvn clean test --also-make --batch-mode
