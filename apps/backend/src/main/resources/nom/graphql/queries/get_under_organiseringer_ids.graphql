query getUnderOrganiseringerIds($nomId: String) {
    orgEnhet(where:  {
        id: $nomId
    }) {
        organiseringer(retning: under) {
            orgEnhet {
                id
            }
        }
    }
}