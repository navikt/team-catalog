query searchForRessurs($term: String!, $statusSelection: StatusSelection!) {
      searchRessurs(term: $term, filter:  {
         statusSelection: $statusSelection
      }) {
        sluttdato
        startdato
        visningsnavn
        navident
      }
}