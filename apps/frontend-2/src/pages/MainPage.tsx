import { css } from "@emotion/css";

const MainPage = () => {
  return (
    <div
      className={css`
        margin-top: 2rem;
      `}
    >
      Vi jobber med forsiden
    </div>
  );
};

export default MainPage;
